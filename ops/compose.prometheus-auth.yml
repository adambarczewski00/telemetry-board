services:
  prometheus:
    # Enable Prometheus web auth/TLS config via a separate config file.
    command:
      - --web.config.file=/etc/prometheus/web.yml
      - --config.file=/etc/prometheus/prometheus.yml
    volumes:
      - ./ops/prometheus/web.yml:/etc/prometheus/web.yml:ro
